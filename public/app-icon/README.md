# App Icon Drop Folder

Drop a square PNG in this folder using this exact name:

- `icon.png`

When you run `npm run dev` or `npm run build`, the app automatically generates:

- `icon.ico` for Windows
- `icon.icns` for macOS

Recommended PNG size: 1024x1024.

Restart the Electron app after changing native icon files.
